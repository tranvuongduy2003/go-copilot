# =============================================================================
# Sealed Secrets Configuration
# =============================================================================
# These are encrypted secrets that can be safely stored in git
# Generate with: kubeseal --format yaml < secret.yaml > sealed-secret.yaml
# =============================================================================

# Placeholder - replace with actual sealed secrets
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: backend-secrets
  namespace: production
spec:
  encryptedData:
    # These values should be encrypted using kubeseal
    # Example: echo -n "your-secret" | kubeseal --raw --namespace production --name backend-secrets
    DATABASE_URL: AgA...encrypted...
    REDIS_URL: AgA...encrypted...
    JWT_SECRET: AgA...encrypted...
  template:
    metadata:
      name: backend-secrets
      namespace: production
    type: Opaque
